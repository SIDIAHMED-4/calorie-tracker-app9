{
  "name": "calorie-tracker-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix packages/client\" \"npm run start --prefix packages/api\"",
    "build": "npm run build --prefix packages/client",
    "start": "npm run start --prefix packages/api"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "sqlite3": "^5.1.6"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "vercel": "^29.0.0"
  }
}
